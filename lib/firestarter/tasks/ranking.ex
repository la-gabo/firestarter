defmodule Firestarter.Tasks.Ranking do
  # generate rank for latest entry

  # 1. get the last item in the list
  # 2. e.g D is the last item in the list
  # 3. find the next optimal character which is higher in value than D
  # 4.

  # generate rank for an entry to be moved as the first rank
  # generate rank for an entry to be moved as the last rank
  # generate rank in between prev and next rank
end
